# ============ 数据库（docker-compose 内会覆盖为 postgres 服务地址）============
POSTGRES_PASSWORD=lumist_pg_pass
POSTGRES_DB=lumist_exam_agent

# ============ 应用配置 ============
API_PREFIX=/api
ENVIRONMENT=production

# JWT 认证（登录/注册用，生产环境务必设置强随机 SECRET_KEY）
SECRET_KEY=change-me-in-production
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=10080

# 若使用外部数据库，可在此设置（docker-compose 部署时一般不需要）
# DATABASE_URL=postgresql+psycopg2://postgres:密码@主机:5432/库名

# 百炼 RAG（可选，为空则不走 RAG）
# WORKSPACE_ID=
# BAILIAN_INDEX_ID=ujsxu342w0

# 调试
# DATA_ROOT=/app/data
# UPLOAD_DIR=/app/data/upload
# LIBRARY_DIR=/app/data/library
# DEBUG_DIR=/app/data/debug
# DB_ECHO=false
